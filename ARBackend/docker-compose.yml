services:
  api:
    build: .
    container_name: ar-campus-api
    ports:
      - "8000:8000"
    environment:
      HF_API_TOKEN: ${HF_API_TOKEN:-}
      HF_MODEL_ID: ${HF_MODEL_ID:-meta-llama/Llama-3.2-3B-Instruct}
    restart: unless-stopped
