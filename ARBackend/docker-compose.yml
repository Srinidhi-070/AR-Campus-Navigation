services:
  api:
    build: .
    container_name: ar-campus-api
    ports:
      - "8000:8000"
    environment:
      HF_API_TOKEN: ${HF_API_TOKEN:-}
      HF_MODEL_ID: ${HF_MODEL_ID:-google/gemma-3-1b-it}
    restart: unless-stopped
